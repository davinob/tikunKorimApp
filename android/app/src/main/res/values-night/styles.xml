<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Night-mode equivalent of values/styles.xml. We intentionally
         use the SAME parchment background in dark mode so the splash
         experience is identical regardless of the system theme. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/parchment</item>
    </style>
</resources>
